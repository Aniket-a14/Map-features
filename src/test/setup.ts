import '@testing-library/jest-dom'

/**
 * Code Explanation:
 * Test setup file for Vitest.
 * Configures the testing environment before running tests.
 *
 * What is Happening:
 * - Imports `@testing-library/jest-dom` to extend Vitest's expect with DOM-related matchers.
 *
 * What to do Next:
 * - Add global mocks (e.g., for ResizeObserver or MapLibre) if needed for other tests.
 */
